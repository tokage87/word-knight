node_modules
dist
.DS_Store
.vite
*.log

# Raw Tiny Swords sprite pack (12 MB of source art). Only the
# processed copies under public/assets/ ship with the game — the
# source pack is needed only to re-run scripts/repack-ui.cjs and
# can be re-downloaded.
sprites/

# Local Claude Code settings (tokens, per-machine prefs).
.claude/settings.local.json

# Generated HTML catalogs — regenerate with
# `npm run curriculum:vacab-list` or by running the asset script
# directly. Keeping them out of git avoids a diff every time the
# underlying data shifts.
/Vacab_list.html
/asset_collection.html

# Personal scratch folder — source material + notes not shipped.
/zadania gpt/
